<!--
  Generated by /revai:attach. Edit freely — this is your project's house rules.
  Replace every {{PLACEHOLDER}}. Leave a command blank with a TODO if it doesn't apply.
-->

# {{PROJECT_NAME}}

## Stack

{{STACK}}  <!-- e.g. Node 20 + TypeScript + Fastify, or Python 3.12 + FastAPI -->

## How to verify your work

Run these to check changes before claiming anything is done. **Evidence before assertions** —
never say something works without running the relevant command and reading its output.

| Purpose | Command |
|---|---|
| Install deps | `{{INSTALL}}` |
| Run the app  | `{{RUN}}` |
| Run tests    | `{{TEST}}` |
| Lint         | `{{LINT}}` |
| Format       | `{{FORMAT}}` |

## Conventions

- Match the style, naming, and structure of the surrounding code.
- Keep units small and single-purpose; a file that's grown large is doing too much.
- Prefer reusing existing functions/utilities over adding new ones.
- Verify before completion (see above); if a step was skipped or a test failed, say so plainly.

## Do not touch

<!-- Generated, vendored, or sensitive paths the AI should leave alone. -->
- {{DO_NOT_TOUCH}}  <!-- e.g. dist/, build/, *.generated.ts, migrations already applied -->
